# 实施与验收记录

记录日期：2026-07-29  
设备：Jetson Xavier NX，JetPack 5.1.4，CUDA 11.4，TensorRT 8.5.2，
`MODE_20W_6CORE`

## 已完成

- 重新标定的 89 张图片和 89 个标签已逐项校验：80 张正样本、9 张真实
  负样本、483 个类别 0 球框；无缺失、越界、非法类别或重复图片。
- 已按连续采集块生成 Train/Validation/Test = 55/17/17，原始 `../data`
  未修改，源文件 SHA-256 已记录在 `dataset/split_manifest.json`。
- YOLO26s 已在 NX GPU 上训练。计划 150 epochs，early stopping 于第 75
  轮结束，最佳轮次为 45，实际 batch=2。
- 已导出两个原始静态端到端 ONNX，并仅对临时副本执行 TensorRT 8.5
  单类别 `index % 1` 等价兼容修改。
- 两个本机 FP16 Engine 已成功构建；运行时实测使用
  `cuda-preprocess+graph` 快速路径。
- 53 项单元测试全部通过。

验证集自动选择置信度为 `0.7799898386`，规则为在 Recall≥0.97 的点中
选择 Precision 最高者。

## 离线精度与速度

| 输入 | 后端 | Precision | Recall | mAP50 | 纯推理 FPS | GPU 均值 |
|---|---|---:|---:|---:|---:|---:|
| 768×480 | PyTorch | 1.000 | 0.952 | 0.9992 | — | — |
| 768×480 | ONNX | 1.000 | 0.952 | 0.9993 | — | — |
| 768×480 | TensorRT FP16 | 1.000 | 0.952 | 0.9993 | 76.247 | 13.114 ms |
| 640×416 | PyTorch | 1.000 | 0.971 | 0.9999 | — | — |
| 640×416 | ONNX | 1.000 | 0.971 | 0.9999 | — | — |
| 640×416 | TensorRT FP16 | 1.000 | 0.971 | 0.9999 | 92.579 | 10.801 ms |

两个 FP16 Engine 相对同尺寸 PyTorch 的 Recall 下降均为 0。两者均通过
P/R/mAP50≥0.95、Recall 下降≤0.01、纯推理≥75 FPS 的离线门槛。RGB-D
完整短测中 768×480 只有 56.04 Hz，已按规则拒绝；`models/default.json`
现选择达到 59.25 Hz 的 640×416。

## 尚待白管场景和 USB 实物验收

336L 已连接并完成 RGB-D 短测；当前没有验收白管和 USB 串口，因此以下
结果仍不能声称已经通过：

- 1280×800@60 MJPEG 完整 Run 连续 10 分钟：采集中位数≥59 FPS、
  推理及串口平均≥58 FPS、P95 端到端延迟≤35 ms；
- 单球连续滚动 5 分钟：实测覆盖率≥97%，含短时预测的有效输出率≥99%；
- 移走球后最多 3 个推理帧进入无球；
- 静止球 1 分钟：平滑后球心 P95 抖动半径≤2 像素；
- 相机和实际 USB 串口断开/恢复现场测试。

现场命令和自动门禁见 `README.md` 的“硬件验收”。若 768×480 完整链路不
满足门槛，应测试 640×416，随后重新运行 `evaluate_engines.py` 和
`select_default.py`；选择器会读取实时报告并拒绝未通过门槛的候选。
# RGB-D 半管升级实测（2026-07-29）

- Gemini 336L：SDK 识别成功，固件 1.6.00，USB 3.2。
- 彩色：1280×800 MJPEG，Jetson `nvjpegdec`；深度：640×400 Y16。
- 厂家彩色/深度内参、畸变和 D2C 外参读取成功。
- 768×480 短测：Capture 60.15 Hz、Inference 56.04 Hz、Pose 29.66 Hz、
  P95 34.71 ms，因推理低于 58 Hz 被拒绝。
- 640×416 短测：Capture 60.03 Hz、Inference 59.25 Hz、Pose 30.01 Hz、
  P95 26.29 ms，通过短测门槛并成为默认 Engine。
- 报告：`artifacts/rgbd_smoke.json` 与
  `artifacts/rgbd_smoke_640x416.json`。
- 当前镜头中没有验收用白色半管，因此管姿态按门禁全部无效；五点精度、
  5 分钟滚动、10 分钟性能和 USB 实物联调仍待现场完成。
